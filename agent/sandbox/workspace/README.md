# Your persistent workspace

This directory belongs to one Headlong identity and persists across its
monolith wakes. Use it for projects, notes, code, experiments, and artifacts.

The append-only trajectory and semantic memories are stored outside this
filesystem and exposed through the Headlong tools. Do not replace them with a
private notes file. Files here are working state and durable outputs.
